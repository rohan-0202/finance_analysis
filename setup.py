from setuptools import find_packages, setup

setup(
    name="finance_analysis",  # Replace with your desired package name
    version="0.1.0",
    description="Finance analysis package",
    packages=find_packages(),
    install_requires=[],
)
